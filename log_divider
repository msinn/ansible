#!/bin/bash

echo |  sudo tee -a $ANSIBLE_LOG_PATH
echo |  sudo tee -a $ANSIBLE_LOG_PATH

echo "---------------   Log Divider   ---------------" |  sudo tee -a $ANSIBLE_LOG_PATH

echo |  sudo tee -a $ANSIBLE_LOG_PATH
echo |  sudo tee -a $ANSIBLE_LOG_PATH
