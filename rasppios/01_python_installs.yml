- name: Install needed packages for Python3
  hosts: all
  become: true
  become_user: root
  tasks:
    - name: Install 'python3-pip' package
      apt:
        name: python3-pip
        state: present

